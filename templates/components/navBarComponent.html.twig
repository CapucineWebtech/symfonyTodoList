<nav class="navbar navbar-expand-lg bg-body-tertiary fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand ms-3" href="{{ url('app_index') }}">
            <img src="{{ asset('build/img/logo.png') }}">
        </a>
        {% if userName is defined %}
            <div class="d-flex">
                <p class="nav-link my-auto me-3">Bonjour {{ userName }}</p>
                <a href="{{ url('app_logout') }}" class="btn btn-danger">Déconnexion</a>
            </div>
        {% else %}
            <div class="d-flex">
                 <a href="{{ url('app_login') }}" class="btn btn-primary me-3">Connexion</a>
                 <a href="{{ url('app_register') }}" class="btn btn-primary">Créer un compte</a>
             </div>
        {% endif %}
    </div>
</nav>
<div class="nav-space"></div>